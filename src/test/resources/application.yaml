camunda:
  bpm:
    auto-deployment-enabled: false
    job-execution:
      enabled: false
    admin-user:
      id: demo
      password: demo
      first-name: demo
      last-name: demo
      email: demo@eorionsolution.com
mybatis-plus:
  mapper-locations: classpath*:**/user-setting-mapper/*Mapper.xml
spring:
  liquibase:
    change-log: db/user-setting-changelog.yml

logging:
  level:
    root: info
    com.baomidou: debug
    org.mybatis: debug
    org.apache.ibatis: debug
    com.eorionsolution: debug